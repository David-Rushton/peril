object torch
    variable is_on = true
    name Mini torch
    description
        if is_on = true
            A small battery powered torch.  It is currently on.
        else
            A small battery powered torch.  It is currently off.
    action turn on is_on = false
    action turn off is_on = true
